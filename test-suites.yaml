version: "3"
services:
  test-suites:
    image: yassmahf98/selenium:latest
    environment:
      - HUB_HOST=hub
      - BROWSER
      - THREAD_COUNT=1
      - TEST_SUITE
      - TEST_OUTPUT
    volumes:
      - ./output/TEST_OUTPUT:/home/selenium-docker/test-output
